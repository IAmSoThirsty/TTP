apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ttp-prod

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - api/deployment.yaml
  - web/deployment.yaml
  - ingress.yaml
  - networkpolicy.yaml

commonLabels:
  app: ttp
  managed-by: kustomize
